# UNIHIKER M10 单文件架构部署指南
版本: 1.4.0

## 概述

本指南详细说明如何在UNIHIKER M10单文件上传限制下部署Troll-vs-Troll防侧翻系统。这是针对UNIHIKER硬件约束的全新架构设计。

## 核心理念

**M10只做一件事：传感器数据采集和传输**
**本地计算机承担所有复杂处理任务**

这种设计完美解决了UNIHIKER只能上传一个文件的技术限制。

## 系统架构

```
┌─────────────────┐    串口通信    ┌─────────────────────┐
│  UNIHIKER M10   │ ◄────────────► │  本地计算机        │
│                 │                │                     │
│ • 传感器采集    │                │ • 数据处理分析      │
│ • 简单显示      │                │ • 风险预测          │
│ • 数据传输      │                │ • 差速控制          │
│ (单文件运行)    │                │ • 机器学习训练      │
└─────────────────┘                │ • 实时可视化        │
                                   │ • 数据存储分析      │
                                   │ (完整功能集合)      │
                                   └─────────────────────┘
```

## 部署步骤

### 第一步：准备UNIHIKER端

1. **文件传输**
   - 将 `unihiker_sensor_collector.py` 传输到UNIHIKER M10
   - 这是M10上唯一的程序文件

2. **运行程序**
   ```bash
   python unihiker_sensor_collector.py
   ```

3. **验证运行**
   - 观察屏幕显示基本传感器数据
   - 确认串口通信指示灯状态

### 第二步：配置本地端

1. **环境准备**
   ```bash
   # 安装必要依赖
   pip install -r requirements.txt
   pip install pyserial
   
   # 确保项目路径正确
   export PYTHONPATH="${PYTHONPATH}:$(pwd)/src"
   ```

2. **运行本地系统**
   ```bash
   python local_complete_system.py
   ```

3. **选择运行模式**
   - 模式1：与UNIHIKER硬件连接
   - 模式2：纯模拟测试模式

### 第三步：建立连接

1. **硬件连接**
   - 使用USB-C线连接UNIHIKER和电脑
   - 系统自动识别COM端口

2. **通信验证**
   - 本地程序显示"串口连接成功"
   - 实时接收传感器数据包
   - 数据处理流水线正常运行

## 功能分工详解

### UNIHIKER端职责（极简设计）

**核心任务**：
- 实时读取内置传感器数据
- 简单的状态信息显示
- 高效的数据打包和传输

**具体功能**：
```python
# 传感器读取
- 加速度计 (accelerometer)
- 陀螺仪 (gyroscope)  
- 光线传感器 (light sensor)
- 按钮状态 (buttons A/B)

# 数据处理
- 实时数据采集 (100Hz)
- 简单数据格式化
- 串口数据传输

# 用户界面
- 基本状态显示
- 连接状态指示
- 简化的传感器数据显示
```

### 本地端职责（完整功能）

**核心任务**：
- 接收并处理所有传感器数据
- 执行完整的防侧翻控制算法
- 提供用户交互界面

**具体功能**：
```python
# 数据接收层
- 串口数据包解析
- 数据队列管理
- 错误处理和重连机制

# 处理分析层
- 传感器数据预处理
- 特征工程和提取
- 多传感器数据融合

# 预测控制层
- 机器学习风险预测
- 差速控制算法执行
- 实时控制决策

# 用户交互层
- 实时状态显示
- 参数配置界面
- 性能监控仪表板

# 学习优化层
- 模型训练和更新
- 数据存储和管理
- 性能分析和报告
```

## 通信协议

### 数据包格式

**传感器数据包**：
```json
{
  "type": "sensor_data",
  "data": {
    "timestamp": 1234567890.123,
    "light": 1024,
    "acceleration": {
      "x": 0.1,
      "y": 0.2,
      "z": 9.8,
      "strength": 10.1
    },
    "gyro": {
      "x": 0.0,
      "y": 0.0,
      "z": 0.0
    },
    "buttons": {
      "A": false,
      "B": false
    }
  },
  "device_id": "UNIHIKER_M10_001"
}
```

**传输协议**：
```
数据帧格式：DATA_START{JSON数据}DATA_END
波特率：115200 bps
数据位：8位
校验位：无
停止位：1位
```

## 性能特点

### 实时性指标
- **传感器采样率**：100 Hz
- **数据传输延迟**：< 50 ms
- **控制响应时间**：< 100 ms
- **显示刷新率**：5 Hz

### 资源分配
**UNIHIKER端**：
- CPU占用：< 30%
- 内存使用：< 50 MB
- 功耗：< 2W

**本地端**：
- CPU占用：可变（根据处理负载）
- 内存使用：200-500 MB
- 功能完整性：100%

## 故障排除

### 常见问题

1. **串口连接失败**
   ```
   检查项：
   - USB线缆连接是否牢固
   - 驱动程序是否正确安装
   - COM端口号是否正确
   - 是否有其他程序占用串口
   ```

2. **数据接收异常**
   ```
   检查项：
   - M10程序是否正常运行
   - 串口参数设置是否匹配
   - 数据包格式是否正确解析
   - 缓冲区是否溢出
   ```

3. **处理性能问题**
   ```
   优化建议：
   - 调整数据采样频率
   - 优化算法计算效率
   - 增加数据处理线程
   - 使用更强大的本地计算机
   ```

### 调试工具

```python
# 启用详细日志
import logging
logging.basicConfig(level=logging.DEBUG)

# 传感器数据监控
def monitor_sensor_data():
    while True:
        data = communicator.receive_data_packets()
        for packet in data:
            print(f"Received: {packet}")

# 性能分析
import time
start_time = time.time()
# 执行操作
end_time = time.time()
print(f"Processing time: {end_time - start_time:.4f}s")
```

## 维护和升级

### 定期维护
1. **数据备份**
   - 定期导出训练数据
   - 保存配置参数文件
   - 备份系统日志

2. **性能优化**
   - 清理历史数据
   - 优化算法参数
   - 更新模型权重

3. **硬件检查**
   - 检查传感器精度
   - 验证连接可靠性
   - 更换老化部件

### 系统升级
1. **软件更新**
   - 下载最新版本代码
   - 备份现有配置
   - 逐步替换核心模块

2. **功能扩展**
   - 添加新的传感器支持
   - 集成额外的控制算法
   - 增强用户交互功能

## 技术支持

如遇到技术问题，请提供以下信息：

- 系统版本号 (1.4.0)
- UNIHIKER固件版本
- Python环境信息
- 错误日志内容
- 具体的故障现象
- 已尝试的解决方法

---
**注意**：本系统采用全新的单文件架构设计，充分发挥了现代边缘计算的优势，既满足了硬件约束又保持了功能完整性。