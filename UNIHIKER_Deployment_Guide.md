# UNIHIKER M10 移植部署指南

## 概述

本文档说明如何在UNIHIKER M10行空板上部署和运行Troll-vs-Troll电子差速防侧翻系统。

## 系统架构

### 双模式运行方案

由于UNIHIKER M10只能同时运行一个文件，我们采用以下分工：

1. **UNIHIKER主程序** (`unihiker_main.py`)
   - 实时传感器数据读取
   - 快速显示更新
   - 紧急控制响应
   - 音频警报播放

2. **本地协作程序** (`local_demo.py` 模式3)
   - 复杂数据处理和模型训练
   - 离线数据分析
   - 模型优化和更新
   - 长期数据存储

## 部署步骤

### 1. 硬件准备

- UNIHIKER M10行空板 × 1
- USB-C数据线 × 1
- 拉杆载具（测试对象）
- 可选：外部传感器扩展模块

### 2. 软件环境配置

#### 在UNIHIKER上：

```bash
# 1. 连接UNIHIKER到电脑
# 2. 确保已刷入最新固件
# 3. 安装必要的Python库
pip install numpy scikit-learn
```

#### 在本地开发环境：

```bash
# 1. 克隆项目代码
git clone <repository-url>

# 2. 安装依赖
pip install -r requirements.txt

# 3. 确保包含以下核心模块：
# - src/ml/rollover_prediction.py
# - src/sensors/data_processor.py  
# - src/control/differential_controller.py
```

### 3. 文件传输

将以下文件传输到UNIHIKER M10：

```
UNIHIKER根目录/
├── unihiker_main.py          # 主程序文件
├── src/
│   ├── ml/
│   │   └── rollover_prediction.py
│   ├── sensors/
│   │   └── data_processor.py
│   └── control/
│       └── differential_controller.py
└── config/
    └── config.py
```

### 4. 运行配置

#### 方案一：独立运行（推荐新手）

在UNIHIKER上直接运行：

```bash
python unihiker_main.py
```

特点：
- 所有功能集成在一个程序中
- 适合快速测试和演示
- 功能相对简化

#### 方案二：协作运行（推荐生产环境）

1. **在UNIHIKER上运行主程序：**
```bash
python unihiker_main.py
```

2. **在本地电脑上运行协作程序：**
```bash
python local_demo.py
# 选择模式3：M10硬件协作模式
```

特点：
- 功能分工明确
- 性能优化更好
- 支持离线训练和数据分析

## 核心功能说明

### UNIHIKER主程序功能

1. **实时传感器监控**
   - 加速度计数据读取
   - 陀螺仪数据读取（如可用）
   - 实时数据显示

2. **风险评估计算**
   - 基于物理模型的侧翻风险预测
   - 多维度数据分析
   - 实时风险等级判定

3. **差速控制算法**
   - 根据风险评估调整轮速
   - 防止侧翻的安全控制
   - 平滑的速度调节

4. **用户界面显示**
   - 240×320像素彩色显示
   - 实时状态信息
   - 风险预警提示
   - 物理参数显示

5. **音频警报系统**
   - 不同级别的声音警告
   - 紧急情况的声音提醒
   - 可自定义的警报音调

### 本地协作程序功能

1. **数据收集与存储**
   - 训练数据采集
   - 历史数据分析
   - 数据质量监控

2. **机器学习训练**
   - 离线模型训练
   - 参数优化
   - 模型性能评估

3. **系统配置管理**
   - 物理参数设置
   - 控制阈值调整
   - 系统校准

## 物理参数配置

根据实际使用的拉杆载具，在`unihiker_main.py`中调整以下参数：

```python
bag_params = {
    'bag_length': 0.5,           # 载具长度(米)
    'bag_width': 0.3,            # 载具宽度(米)  
    'bag_height': 0.7,           # 载具高度(米)
    'center_of_gravity_height': 0.4,  # 重心高度(米)
    'wheel_radius': 0.1,         # 轮子半径(米)
    'wheel_mass': 0.5,           # 单个轮子质量(千克)
    'bag_total_mass': 5.0        # 整体质量(千克)
}
```

## 使用说明

### 启动步骤

1. **硬件连接**
   - 将UNIHIKER通过USB-C连接到电源或电脑
   - 确保传感器模块正确安装

2. **程序启动**
   - 方案一：在UNIHIKER上运行 `unihiker_main.py`
   - 方案二：分别在两个设备上启动对应程序

3. **系统检查**
   - 观察显示屏是否正常显示
   - 检查传感器数据是否稳定
   - 确认控制算法正常工作

### 操作界面

显示屏信息布局：
```
第1行：系统标题 "Troll-vs-Troll"
第2行：功能描述 "防侧翻安全系统"  
第3行：系统状态 "NORMAL/WARNING/EMERGENCY"
第4行：风险等级和百分比
第5-8行：传感器数据详情
第9行：控制状态
第10-11行：左右轮速度
第12-13行：物理参数信息
第14行：系统时间
第15行：警告信息（如有）
```

### 安全注意事项

1. **使用前检查**
   - 确认所有参数设置正确
   - 测试传感器响应是否正常
   - 验证控制算法工作状态

2. **运行中监控**
   - 注意观察风险等级变化
   - 留意音频警报提示
   - 监控系统状态指示

3. **异常处理**
   - 如遇紧急情况立即停止使用
   - 记录异常数据用于后续分析
   - 联系技术支持获取帮助

## 故障排除

### 常见问题

1. **程序无法启动**
   - 检查Python环境和依赖库
   - 确认文件路径正确
   - 验证硬件连接状态

2. **传感器数据异常**
   - 检查传感器模块连接
   - 重新校准传感器参数
   - 排除电磁干扰源

3. **显示问题**
   - 确认GUI库正确导入
   - 检查显示刷新频率设置
   - 验证屏幕驱动程序

4. **控制响应延迟**
   - 优化算法计算效率
   - 调整控制更新频率
   - 检查系统资源占用

### 调试工具

```python
# 启用详细日志输出
import logging
logging.basicConfig(level=logging.DEBUG)

# 传感器数据监控
def debug_sensor_data():
    accel, gyro = system.read_sensors()
    print(f"Accel: {accel}, Gyro: {gyro}")

# 性能分析
import time
start_time = time.time()
# 执行操作
end_time = time.time()
print(f"执行时间: {end_time - start_time:.4f}秒")
```

## 维护与升级

### 定期维护

1. **数据备份**
   - 定期导出训练数据
   - 保存配置参数文件
   - 备份系统日志

2. **性能优化**
   - 清理历史数据
   - 优化算法参数
   - 更新模型权重

3. **硬件检查**
   - 检查传感器精度
   - 验证连接可靠性
   - 更换老化部件

### 系统升级

1. **软件更新**
   - 下载最新版本代码
   - 备份现有配置
   - 逐步替换核心模块

2. **功能扩展**
   - 添加新的传感器支持
   - 集成额外的控制算法
   - 增强用户交互功能

## 技术支持

如遇到技术问题，请提供以下信息：

- UNIHIKER固件版本
- Python环境信息  
- 错误日志内容
- 具体的故障现象
- 已尝试的解决方法

---
**注意：** 本系统仍在开发阶段，请在安全环境下进行测试，避免在实际交通环境中使用未经充分验证的版本。